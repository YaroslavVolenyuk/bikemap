PGHOST=localhost
PGUSERNAME=admin
# PGPASSWORD has been removed from this file. Set the real password in a local-only file (e.g. .env.local) or in your deployment secrets.
PGPASSWORD=__REDACTED__
PGDATABASE=bikemap

# GRAPHHOPPER_API_KEY removed from repository. Set the real key in a local-only .env.local or in your deployment environment.
GRAPHHOPPER_API_KEY=__REDACTED__
